/** Shared Recharts tooltip chrome for WW360 dashboards. */
export const ww360ChartTooltipStyle = {
  contentStyle: {
    borderRadius: 8,
    border: '1px solid #e2e8f0',
    fontSize: 12,
    boxShadow: '0 4px 12px rgba(7,17,31,0.08)',
  },
  labelStyle: { fontWeight: 600, color: '#0f172a' },
} as const;
