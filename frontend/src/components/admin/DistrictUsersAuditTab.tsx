/**
 * District users — embedded in District Admin Hub.
 */
import DistrictUserManagement from '@/components/admin/DistrictUserManagement';

export function DistrictUsersAuditTab() {
  return <DistrictUserManagement embedded />;
}
