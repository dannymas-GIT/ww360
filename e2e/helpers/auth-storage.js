/**
 * Optional: log in once and save storage for reuse.
 *   PW_SAVE_STORAGE=1 npx playwright test e2e/helpers/login-once.spec.js
 * Then set PLAYWRIGHT_STORAGE=.auth/user.json in CI or locally.
 *
 * Implement login-once.spec.js in your app (paths + credentials from env).
 */
module.exports = {
  // Placeholder — implement per-app with __APP_PREFIX___TEST_EMAIL / _TEST_PASSWORD
};
