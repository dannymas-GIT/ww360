# Core FastAPI & Web Server
fastapi==0.135.2  # Security update — pulls starlette>=0.49.1 (CVE-2025-62727)
uvicorn[standard]==0.35.0

# Database & ORM
sqlalchemy==2.0.42
psycopg2-binary>=2.9.0
alembic>=1.11.1

# Data Validation
pydantic==2.11.7
pydantic-settings>=2.0.0,<2.3.0
email-validator>=2.0.0
PyYAML>=6.0.0

# CORS
python-multipart>=0.0.6

# Testing
pytest>=7.3.1
httpx>=0.24.1

# Authentication & Security
passlib[bcrypt]==1.7.4
PyJWT[crypto]>=2.12.0  # Replaces python-jose (unmaintained); fixes CVE-2026-32597
bcrypt==4.0.1
python-dotenv>=1.0.0
cryptography>=46.0.5  # Security update (CVE-2026-26007)

# HTTP / async
aiohttp>=3.13.3  # Security update — 8 CVEs fixed vs 3.12.x
requests>=2.33.0  # Security update (CVE-2026-25645)

# Data processing
pandas>=2.0.0
pypdf>=5.0.0
pdfplumber>=0.11.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Fuzzy string matching for well/contaminant name drift in lab CSVs
# (rapidfuzz is C++-backed, ~10x faster than fuzzywuzzy / difflib, MIT,
# pure-wheel — no native compile step on the runner).
rapidfuzz>=3.6.0

# Notification Services
twilio>=8.0.0
APScheduler>=3.10,<4

# Two-Factor Authentication
pyotp>=2.8.0
qrcode[pil]>=7.4.0

# Document Processing & Web Scraping
beautifulsoup4>=4.12.0
reportlab>=4.0.0

# Data Visualization & Charts
matplotlib>=3.7.0
seaborn>=0.12.0

# AI & Machine Learning
openai>=1.0.0

# Microsoft authentication (Graph / Office 365)
msal>=1.28.0

# Azure Key Vault & Identity
azure-keyvault-secrets>=4.7.0
azure-identity>=1.15.0
azure-core>=1.38.0  # Security update (CVE-2026-21226)

# Azure Blob Storage (self-hosted tutorial video storage)
azure-storage-blob>=12.19.0

# Transitive pins — keep above minimum safe version
pyasn1>=0.6.3  # CVE-2026-30922

# Development tools
black>=26.3.1  # Security update (CVE-2026-32274)
isort>=5.12.0
openpyxl
